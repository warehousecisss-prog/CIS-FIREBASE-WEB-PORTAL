# Reference material (read-only)

Vendored snapshots from the original Apps Script repo (`../SRC/src`, its own git repo).
These are the porting contract. Current as of **SCHEMA.md v17 / 2026-08-27**.

| File | Purpose |
|---|---|
| `SCHEMA.md` | Canonical operational contract — status lifecycles, column maps, invariants. Source of truth. |
| `AUDIT_2026-08-24.md` | Deep audit + phased remediation; documents the critical silent-write-failure bugs. |
| `CHANGELOG.md` | Dated fix/feature history from the original. |
| `README.md` | Original architecture overview. |

The full original source stays at `../SRC/` (gitignored here) as a live cross-reference.
